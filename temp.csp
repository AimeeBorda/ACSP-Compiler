transparent normal 

Q = normal((let R = l?id -> (map(l,id) /\ R) 
 within (SKIP/\ R)[|{| l|}|] R) \ {| l|})
R = let x = 5 within l!0 -> SKIP
S(x) = SKIP

assert Q :[livelock free [FD]]

channel l : {0..0}

map = \ chName,id @ if chName == l and id == 0 then S(x) 
else SKIP
